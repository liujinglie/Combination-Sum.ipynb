class Solution:
    def reverseWords(self, s: str) -> str:
        w = s.split()
        w2 = []
        x = len(w)-1
        while x>=0:
            w2.append(w[x])
            x-=1
        return ' '.join(w2)
